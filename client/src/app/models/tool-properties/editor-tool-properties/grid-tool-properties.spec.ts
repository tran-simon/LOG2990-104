import { GridToolProperties } from './grid-tool-properties';

describe('GridToolProperties', () => {
  it('should create an instance', () => {
    expect(new GridToolProperties()).toBeTruthy();
  });
});
