import { NumericProperty } from 'src/app/models/tool-properties/props/numeric-property/numeric-property';

describe('NumericProperty', () => {
  it('should create an instance', () => {
    expect(new NumericProperty()).toBeTruthy();
  });
});
