import { SprayToolProperties } from './spray-tool-properties';

describe('SprayToolProperties', () => {
  it('should create an instance', () => {
    expect(new SprayToolProperties()).toBeTruthy();
  });
});
