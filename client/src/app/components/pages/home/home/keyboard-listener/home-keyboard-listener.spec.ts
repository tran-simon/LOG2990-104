import { HomeKeyboardListener } from './home-keyboard-listener';

describe('HomeKeyboardListener', () => {
  it('should create an instance', () => {
    expect(new HomeKeyboardListener()).toBeTruthy();
  });
});
