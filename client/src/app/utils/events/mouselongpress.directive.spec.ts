import { MouselongpressDirective } from './mouselongpress.directive';

describe('MouselongpressDirective', () => {
  it('should create an instance', () => {
    const directive = new MouselongpressDirective();
    expect(directive).toBeTruthy();
  });
});
